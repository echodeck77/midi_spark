# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode — user state (never shared)
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Xcode — build products
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip
*.xcarchive

# Swift Package Manager
.build/
.swiftpm/
Packages/

# Playgrounds
timeline.xctimeline
playground.xcworkspace

# Fastlane / CocoaPods / Carthage (unused today; harmless guards)
Pods/
Carthage/Build/
fastlane/report.xml
fastlane/screenshots

# Claude↔Claude message passing (local only — inbox/outbox, never tracked)
# _dear_claude/ = my outbox (messages TO the design-side Claude)
# _dear_claude_code/ = my inbox (messages FROM the design side, addressed to Claude Code)
/_dear_claude/
/_dear_claude_code/
# _dear_paul/ = notes to the user (local scratch channel, never tracked)
/_dear_paul/
