# SPEC → Code — EUCLID VARIATIONS (Paul's session, 2026-08-22 —
# captured, awaiting his word per item)

## §0 · Ground truth (for the manual)
EUCLID = whole-chord striker: K hits spread over N steps, every
hit strikes the full set. **Per-note euclid EXISTS = WEAVE
EUCLID** (each rank its own pattern, denser on top) — the right
family, since per-note clocks are WEAVE's whole nature.

## §1 · EUCLID gains PICK (what each hit strikes)
**PICK: ALL (today) | CYCLE | LOW | HIGH | RANDOM** — the TUTTI
vocabulary on euclid's hits:
- CYCLE = **the euclid-arp**: sparse hits that WALK the chord.
- LOW = the euclidean bass pulse. RANDOM = seeded scatter.
One chip; the card's biggest musical upgrade.

## §2 · INVERT (one chip, the classic)
Play the N−K RESTS instead — the anti-pattern. Two cells, one
inverted = perfect interlock from one chord.

## §3 · WEAVE EUCLID gains AUTHORING
Per-rank **K** and **ROTATE** (small lanes over the ranks —
today's auto-assignment becomes the default the lanes edit).
Offset ranks = interlocking parts from one hand.

## §4 · K as a MOD target (free — name it, don't build it)
The internal-target law already covers HITS: MOD→THIS CHAIN:
HITS = the pattern breathing 3→7 over passes. A manual line.

## §5 · ★ THE EUCLID BRUSH (the meta-win)
An **e-fill button on every MATRIX ROW and SLIDER LANE**: "fill
k-of-N euclidean" (tap e, dial k, the row fills; rotate after).
Euclid stops being only a processor and becomes an AUTHORING
TOOL across the whole widget language — euclidean accents,
euclidean mutes, euclidean hockets in the dest matrix, euclidean
odds in the chance lane. One helper, every grid.
— design-side Claude

## §6 — THE PANELS (the control lists, canon)
**EUCLID**: HITS FROM SET|CHORD · HITS 1–16 · STEPS 2–16 ·
ROTATE · **PICK ALL|CYCLE|LOW|HIGH|RANDOM** · **INVERT** ·
SPAN ladder · (crown + IN/OUT strips + eye, as all cards).
**WEAVE EUCLID**: VOICES · STEPS · **PER-RANK K LANE** (ships
pre-filled with the auto ladder — works untouched, authors on
touch) · **PER-RANK ROTATE LANE** (the interlock) · LENGTH ·
NEW CHORD R|C|F · (crown + strips + eye).
**The BRUSH**: on no panel — the e-fill lives on every matrix
row and slider lane app-wide (dial k, fill, rotate after).

## §7 — THE REAL ASK (Paul, 2026-08-22): A EUCLID LANE PER NOTE —
## §6's WEAVE EUCLID panel superseded
- **THE RANK-LANE MATRIX**: rows = the held notes (to VOICES) ·
  columns = STEPS (2–16) · every cell a TOGGLE — each note's
  rhythm is its own visible, editable lane. The polyphonic x0x:
  every held note a drum lane.
- **EUCLID = THE FILL, per row**: each lane carries its e-brush
  chip (k + rotate inline) — tap e, dial k, the row fills
  euclidean; rotate slides it. **Ships auto-filled** with the
  density ladder (denser on top) — plays untouched, authors on
  touch.
- **Hand edits are legal and expected**: the fill is a starting
  point; toggled cells make the pattern yours (stored per rank —
  config, deterministic, scene-captured like all params).
- Remaining controls: VOICES · STEPS · LENGTH · NEW CHORD R|C|F ·
  SPAN ladder · crown + strips + eye. The per-rank K/ROTATE
  stepper-lanes from §6: superseded by the matrix (the brush
  chips carry k and rotate now).
- Family note: WEAVE DRAWN sequences per-note RATES; this
  sequences per-note STEP PATTERNS — siblings, distinct, both
  true to WEAVE's nature (per-note clocks).

## §8 — THE ROZETA RHYTHM ALIGNMENT (Paul's reference,
## 2026-08-22, verified: "euclidean drum sequencer with
## mutations per instrument and polyrhythms" — two corrections
## to §7)
1. **PER-LANE STEPS (polyrhythm)**: each note-lane owns its OWN
   cycle length N (2–16) — §7's shared STEPS demoted to the
   default a lane inherits until touched. Lanes of different
   lengths cycle against each other (3-against-4-against-16 from
   one chord). Render: each row drawn at its own length with a
   wrap mark (the Rozeta look); the engine cost = per-lane
   rowStep % N_lane (the polymeter law, per lane).
2. **PER-LANE MUTATE**: a small die per row — seeded, lane-
   scoped variation (our MUTATE grammar at lane grain; the whole-
   card die remains for all-lanes rolls).
Each lane's full kit: **K · N · ROTATE · 🎲 · the e-fill · hand
toggles**. The fusion stated for the record: Rozeta Rhythm's
architecture + our derivation = lanes whose KIT IS THE HELD
CHORD — the polyrhythmic euclidean sequencer that follows
harmony. Nothing on iOS does both.

## §9 — PAUL'S SIMPLICITY RULING (2026-08-22): the TARGET chip —
## §7/§8's rank-lane matrix WITHDRAWN
- **EUCLID gains ONE chip: TARGET: ALL (today) | NOTE 1–8** —
  the euclid strikes only the targeted pool note.
- **Multi-note = MULTIPLE EUCLID CELLS**, each targeted — the
  grid IS the lanes. Everything §8 wanted arrives free from
  existing machinery: per-cell STEPS = polyrhythm · per-colour
  MUTATE = the lane die · per-cell ROTATE · colours/scenes/
  banking all apply. Zero new UI beyond the chip.
- The rank-lane matrix editor (§7, §8's lane-kit) is WITHDRAWN
  as over-engineered — tombstoned here with its reason: the
  house already had the lane structure; I built a second one.
- Survivors, unaffected: PICK (§1) · INVERT (§2) · K-as-MOD
  (§4) · **the e-brush (§5)** — the brush serves OTHER matrices
  and lanes app-wide regardless. WEAVE EUCLID stands unchanged
  as the one-cell auto version (the compact option; the
  targeted-cells approach = the authored one).

## §10 — THE LINES MODEL (Paul, 2026-08-22 — supersedes §9's
## multi-cell suggestion, which broke on staging exclusivity:
## cells are picks; three rows for one rhythm spends parts.
## Second correction this session, both his — noted.)
- **EUCLID = up to 8 LINES in one card.** Each line, one compact
  row of the SAME simple controls: **TARGET ▾ (ALL | note 1–8) ·
  K · STEPS · ROTATE** (+ a per-line 🎲 if the glass wants it).
  **ADD LINE** appends; × removes; line 1 ships as TARGET ALL =
  today's card exactly (a one-line instance; migration
  invisible).
- **Per-line STEPS = the polyrhythm** (each line its own cycle;
  the per-line modulo). PICK (§1) and INVERT (§2) apply per
  line. The window draws the combined pattern.
- Precedent: HARMONIZE's voice rows — a stack of identical
  control lines is furniture the house already owns.
- One cell now holds the whole interlocking engine: three lines
  = kick/hat/pulse from one chord, one colour, one machine —
  banked, muted, and scened as a unit, which is what it
  musically is.

## §11 — RATIFIED (Paul, 2026-08-22)
The survivors are law: **THE LINES MODEL (§10) · PICK (§1) ·
INVERT (§2) · K-as-MOD-target (§4, the manual line) · THE
E-BRUSH (§5, app-wide)**. The two tombstones (§7/§8 matrix,
§9 multi-cell) stand as records. WEAVE EUCLID unchanged as the
compact auto sibling.
