# SPEC → Code — ARP: two additions (Paul, 2026-08-22 — captured,
# awaiting his word)

## 1 · OCT DIRECTION: UP (today) | DOWN
One toggle beside the OCT range. Orthogonal to PATTERN: the
pattern orders notes WITHIN a lap; OCT DIRECTION orders the LAPS
(DOWN = start at the top octave, descend — "up the chord, down
the octaves"). All patterns compose with it unchanged.

## 2 · RANDOM ANCHOR: OFF | LOW FIRST | HIGH FIRST
A chip shown only when PATTERN = RANDOM. Each CYCLE (one full
traversal of the pool × octaves) opens on the anchor note
(lowest/highest); the remaining steps shuffle (seeded, as all
dice). Rooted chaos: the downbeat lands, the body scrambles.
Definition pin: the anchor applies at each WRAP (a new traversal),
not per bar — pool-size changes re-anchor naturally.
Kinship note: EUCLID's PICK RANDOM may want the same anchor chip
someday; if so, same words, same law.

## RATIFIED (Paul, 2026-08-22)
Both law as written: OCT DIRECTION UP|DOWN and RANDOM ANCHOR
OFF|LOW|HIGH (wrap-scoped, seeded, conditional chip).
