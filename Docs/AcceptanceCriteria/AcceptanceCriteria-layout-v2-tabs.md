# CAPTURE → Code — LAYOUT v2: THE TAB ERA (Paul's brain dump,
# structured; direction not yet AC) — 2026-08-04
_"The basis for how I want it to look and feel." Read-backs marked
[RB]; open confirms marked [?]._

## 1. THE TAB HEADER (supersedes PERFORM|EDIT)
Tabs: **GRID · PROCESSORS · MIDI RECEIVERS · MIDI EMITTERS · MACROS
(tbi) · AUTOMATION (tbi)**. Rationale: mode-toggles + in-grid
overlays wreck muscle memory; every surface gets ONE permanent
address. [RB] The overlay rule's grid-takeovers become PAGES; its
residue = within-page popups only. Existing designs map:
PROCESSORS = the edit page · MIDI EMITTERS = the RACK matrix ·
AUTOMATION = the lanes editor · MACROS = the 24-slot management +
A/B bindings · MIDI RECEIVERS = door depth (cog's per-door config
promotes to a page; the cog keeps only true globals).

## 2. THE HEADER GAINS THE CLOCK
Main RATE (step) + SWING move to the header — global truths reachable
from every tab; rates via a pop-up.

## 3. THE GRID PAGE
- **Top band** [RB, confirm]: RECEIVERS box moves LEFT (the CONTROLS
  section dies — STEP/SWING went to the header) · **8 MACRO SLIDERS**
  centre · **8 MACRO BUTTONS** top-right (the FLOW-succession slot).
- **Bottom-left: SINGLE | MULTI** toggle (the PERFORM|EDIT segmented
  style, repurposed): **SINGLE** = one cell speaks per column, tap
  switches (LADDER's semantics) · **MULTI** = normal layering, tap =
  mute/unmute (the old MUTE-arm behaviour as the default tap).
- **The verb cluster dies entirely**: MUTE → MULTI's tap · LADDER →
  SINGLE · **SELECT → RETIRED** [? one word confirms — this resolves
  the floating ruling] · HOLD → localised (below).
- Emitters remain the bottom band; the page reads left→right as
  signal flow: doors → grid → wires.

## 4. HOLD LOCALISES
Global HOLD dies. Each SLIDER SET gains its own small hold latch
(macro sliders · input velocity faders · output velocity faders ·
master) — spring semantics per group, latched locally.

## 5. THE GLOBAL SECTION
A handful of FREEZE / STUTTER-class gestures (the global-gestures
spec: FREEZE · STRIP · SLICE · THIN) get a compact home in the
master/global area.

## 6. Supersessions this direction implies (for the record)
PERFORM|EDIT toggle · the verb cluster · global HOLD · the overlay-
as-takeover pattern (→ tabs) · CONTROLS box. LADDER-the-toggle
becomes SINGLE [? naming: does LADDER survive as the concept/preset
name while the toggle reads SINGLE|MULTI? — lean yes].

## 7. Open [?] set for Paul
① Top-band order as read back (receivers L · sliders C · buttons R)?
② SELECT = retire, confirmed by this?  ③ MULTI-tap = mute — where do
cell TRIGGER actions (the roster) eventually fire, or does tap=mute
stand as the grid's law?  ④ LADDER naming survival as above.
— design-side Claude
