# INSTRUCTIONS → Code — BUILD PAGE ITERATION 2: verbs live at their
# sources · the target decides the verb (Paul + design, 2026-08-11)

## 1. THE PLACEMENT PRINCIPLE (Paul's instinct, made the rule)
**A verb lives at its SOURCE, pointing at its destination.**
- **APPLY TO STAGING** leaves the right column — it's the
  MACHINE's verb (the build sentence's final word): it joins the
  machine/palette side, arrow pointing right at staging.
- **PLAY THIS CELL** = yes, the focus grab for the machine zone
  (pulls the voice left, per the focus model) — so it moves LEFT:
  the machine's own play button, first thing in its row.
- **APPLY TO PLAY** = STAGING's verb → **one button UNDER THE
  STAGING GRID**, per Paul's option B.

## 2. ★ THE SIMPLIFICATION — THE TARGET DECIDES THE VERB
FLATTEN and COPY ROWS stop being user choices: press **APPLY TO
PLAY** → the play grid arms (bands pulse) → **tap a LANE =
FLATTEN** (a lane can only take a line) · **tap a LADDER = COPY
ROWS** (a ladder takes rows, actives live) · tap FREE = the picks
land as takes (the menagerie stocks). The band's nature
disambiguates; two buttons dissolve into one gesture. Edge case
(flatten INTO a ladder as one row): long-press the ladder = the
alternate — rare, discoverable, never in the way.

## 3. THE MACHINERY BAR = THE COMPLETE BUILD SENTENCE (Paul's
## reorder, endorsed with signal-order)
The bottom bar absorbs the left column, reading left→right as the
signal flows:
**[● PLAY CELL] · [PART 1 ▾] · [INPUT: R1⌨ R2 R3 R4] · [THE CAST
(the part's swatches, compact)] · [the chain slots + ghost] ·
[OUTPUT: A B C D]**
— play-and-focus first (the lamp's handle), then who/where/what/
to-where. The left column DISSOLVES → **the grids gain its width**
(staging and play both breathe; the mock's tightest constraint
relaxes). LITTER parks for later placement, per Paul.

## 4. BELOW STAGING = the staging verbs
**[APPLY TO PLAY] · [MUTATE] · [🎲 re-roll]** — the workshop's own
strip, under its own grid. The right column empties and can go.

## The page, re-read
Bottom bar = build the machine (left-lit when playing) → staging
centre = stock and play (the ● PLAYING frame as shipped) → APPLY TO
PLAY under it → the play grid right, arming to receive, its bands
choosing the verb. Three zones, every verb at its source, the
light still never lying.
— design-side Claude
