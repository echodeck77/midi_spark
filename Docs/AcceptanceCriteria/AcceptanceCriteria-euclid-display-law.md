# INSTRUCTIONS → Code — THE EUCLID DISPLAY LAW (Paul, 2026-08-27
# — new day, new doc; captured, awaiting his word)

## The law
**Wherever euclid controls appear, the COMPUTED PATTERN renders
adjacent — as a GRID ROW, never a circle.** The K-of-N result
drawn as a slice-row of cells (hits lit), directly under/beside
its K·N·rotate controls.

## Why grid (the reasoning, on record)
Time-as-columns is the app's one motion grammar (playheads sweep
L→R everywhere); the cell-row is the native rhythm render (slice
rows · matrices · lanes); and under the ratified ROTATE ruling
**the display IS the control** — dragging the pattern row
rotates it (wrap at the edges), with result-ghosting (idea 17)
while nudging and the live playhead cell (idea 15) composing in.
A circle draws cyclicity; the grid makes it usable.

## Where it applies (the surfaces)
- **EUCLID (the card)** — the pattern row under HITS/STEPS.
- **THE LINES MODEL** — each line renders ITS OWN pattern row
  (per-line K·N·rotate each get their truth).
- **WEAVE EUCLID** — each rank lane shows its computed pattern.
- **THE ARP MASK** — the K-of-N pair gets its row beneath
  (appearing with the kit when K<N, per the animate-in law).
- **THE E-BRUSH popover** — a live preview row while dialing K,
  before the fill commits.
Small (slice-row height); hue = the owning colour; read-only
cells except the drag-rotate.
— design-side Claude

## RATIFIED (Paul, 2026-08-27)
The display law is law as written — the grid row, all five
surfaces, drag-to-rotate as the display's own interaction.
