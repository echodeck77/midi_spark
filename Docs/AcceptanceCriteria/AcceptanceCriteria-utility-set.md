# SPEC → Code — THE UTILITY SET (Paul, 2026-08-22: "simpler
# processors" — captured; OCTAVE is his direct ask, the rest are
# the audited siblings. A seventh catalog group: UTILITY.)

- **OCTAVE** — one knob, ±3: this chain plays low or high.
  Pool-safe by construction (pitch-class preserved — the thesis
  untouched). The gap named: door-OCT moves every chain on an
  input; OCTAVE moves ONE.
- **TRANSPOSE** — semitones ±24 (+ an OCT row if wanted). Legal
  by the HARMONIZE precedent (fixed intervals already exist);
  the panel notes honestly: "moves notes off the held chord."
  The chromatic crowd's tool; OCTAVE stays the simple card.
- **CHANNEL** — CH: WIRE (default) | 1–16: this chain exits on a
  different MIDI channel than its wire's default — multitimbral
  from one output (one synth, two timbres; Drambo/multi-channel
  rigs). Sub-address, so it announces lightly ("sends on channel
  n") — the override family's plainest member.
- **NUDGE** — ± ticks/steps: a pure time offset on the stream
  (transforms; does NOT drive — SHIFT strikes, NUDGE slides
  what's already there). The TAP's natural companion:
  [TAP → NUDGE] = slapback layers from one machine.

Audited and NOT added (duplicates): range-keep = SPLIT · thinning
= CHANCE-KEEP · octave-doubling = HARMONIZE ±12 · sustain = the
door's job (the standing decline). MIRROR/INVERT (rank-flip) =
parked as spice, not utility.
Catalog: all four join a new **UTILITY** group; one-liners are
their own descriptions above. All await Paul's word; OCTAVE is
ratify-ready the moment he says so.
— design-side Claude

## RATIFIED (Paul, 2026-08-22)
All four — OCTAVE · TRANSPOSE · CHANNEL · NUDGE — law as written,
with the UTILITY catalog group. Build order Code's discretion;
OCTAVE first is the natural lean (the direct ask, the one-knob
proof of the group).
