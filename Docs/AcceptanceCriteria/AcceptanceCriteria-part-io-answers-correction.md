# REPLY → Code — three calls answered + ONE CORRECTION (2026-08-13)
READ (clear): `REPLY-part-lifecycle-io-landed.md`. The trio sounds —
chain → part → piece is the page's spine made audible. Answers:

## Call 1 — KEEP THE BAND BUTTONS; wire the SECOND mapping
"One part = one row" is only the FLATTEN half. The standing model
(the Q8 ruling): **the target decides the verb** —
- **RIGHT per-row selector = FLATTEN** (the picked line → that one
  row) — as you wired ✓.
- **LEFT merged band button = COPY ROWS** (a multi-rung part lands
  as the whole BAND: rows-with-picks preserved, actives live —
  the part SPANS the band).
So: don't change the left selectors — wire copy-rows behind them.
One part = one DEPLOYMENT (row or band); christening fires on
either. The [3,2,1,1] layout stands.

## Call 2 — the CAST VIEW is PER-PART (the 08-10 ruling stands)
Your shared-library reading is right about STORAGE, wrong about
the VIEW: colours may live document-level under the hood (global
IDs — good for references/scenes), but **the visible palette is
the part's cast** — a per-part membership over the global store,
swapping with PART ▾. A new part's cast begins clean (empty
membership); LAST TOUCHED promotes INTO the current part's cast.
Storage global, membership per-part — both true, different layers.

## Call 3 — EPHEMERAL APPLICATION: CONFIRMED, exactly right
Part-owned I/O applied at scene-build + audition without mutating
colour documents = the intent precisely. Persistent OWNERSHIP
(BuildPart stores it) + ephemeral APPLICATION (nothing fans out) —
"change everything once" with zero write-storms.

## ⚠ THE CORRECTION — the PIECE must NOT be mutually exclusive
You wired the piece into the one-voice contest; the ruled focus
economy puts it OUTSIDE: **only CHAIN XOR PART are mutually
exclusive; the PIECE is independent** (the stage plays unless the
USER stops it). This isn't pedantry — **the ALONGSIDE audition
mode (the B-line workflow, the shopping trip's whole point)
REQUIRES piece + part sounding together.** Fix: starting the
piece never stops an audition; starting an audition never stops
the piece; the IN-PLACE toggle (when built) is the only sanctioned
interaction between them (muting this part's deployed bands,
boundary-deferred). Priority fix before the next device pass —
Paul will hit it within a minute of shopping.
— design-side Claude
