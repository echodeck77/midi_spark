# INSTRUCTIONS → Code — THE EVENING DOC (Paul, 2026-08-27):
# today's RATIFIED changes, compiled. Ratified-only by his rule.

## §1 — THE STANDARD PANEL ANATOMY (ratified today; the new law)
Every processor panel takes ONE shape, read top→bottom as
"what I am → what I receive → how I work → what I emit":

**HEADER** (the crown + two new standards):
- The existing crown (swatch · label · BYPASS(hold=A/B) · DELETE
  · CANCEL · DONE) + the one-line sub-header (increment-2) + the
  eye.
- **SOURCE: CHAIN | MIDI IN | BOTH** (default CHAIN =
  byte-identical today). "MIDI IN" = the ROW'S OWN door (never a
  picker — the row badge owns which). The flow line draws the
  truth: a MIDI-IN-sourced stage threads from the door glyph.
  Unlocks parallel paths without TAP: [ARP → HARMONIZE(src:
  MIDI IN)] = the walk + the pad beneath, one chain.
- **OCT ◀n▶** — this stage's own voice shifted (±3), the nudge
  grammar, dimmed at 0. **BOTH octaves stand**: the header OCT =
  the stage's voice; the OCTAVE card = positional stream
  transform. Distinct jobs, ruled coexisting.

**THE IN STRIP** — directly under the header (the ratified truth
strip; empty-state teaches).

**THE BODY** — hero first (accent bar), then per the six rules.

**THE FOOTER** (ratified today, in this order):
1. **The frame row** (pattern cards only): **GRID · ROTATE(◀n▶)
   · SPAN** — fixed order AND fixed place; hands learn one
   location across every pattern card.
2. **OPTIONS** — the ★ toggles as small chips under one label
   (INVERT · FIT 1 BEAT · DRY NOTE · SPILL · RAKE · kin).
3. **The "pairs well" line — RATIFIED IN** (un-parked): one dim
   row, e.g. "pairs well: → LENGTH · ← SPLIT", from the pairing
   catalog's per-card data.
4. **The OUT roll** — the floor: what this stage emits, live.

## §2 — TODAY'S OTHER RATIFIED FERRIES (each has its own file;
## one-line index)
- `INSTRUCTIONS-euclid-display-law.md` — every euclid control
  gets its computed pattern as an adjacent GRID ROW (never a
  circle); the row drag-rotates; five surfaces listed.
- `SPEC-span-sustain.md` — **STRIKE: PER CELL | PER SPAN** on
  every span-ladder holder (fire at span origin, hold through —
  index-span vs strike-span; CARRY generalised).
- `FERRY-strike-anchor.md` — **revert the internal
  transport-when-stopped**; the hardware-arp feel lands as
  **RETRIG = strike-anchored, immediate** (phase =
  time-since-strike, replay-exact). Context section included.

## §3 — Explicitly NOT in this doc (so nothing is inferred)
- ARP CYCLES + THEN + the phrase family (`SPEC-arp-phrase.md`) —
  discussed today, **NOT yet ratified**; captured-awaiting-word.
- Standing reminder: `FERRY-passage-law.md` (earlier session)
  still awaits its copy — it unblocks reconcileBypass + the I/O
  gate and composes with the strike-anchor revert.
— design-side Claude

## SIGNED OFF (Paul, 2026-08-27) — this compilation is approved
as the evening handover; §3's fences stand as written.
