# INSTRUCTIONS → Code — BUILD PAGE ITERATION 6 (Paul, 2026-08-13)
# The restructure, ruled. Supersedes: the footer chain placement
# (+ its §5 tombstone — in-column was the unconsidered third
# option); §10's single console (it SPLITS, below).

## 1. THE LEFT COLUMN COMPLETES THE MACHINE
Top → bottom: **PLAY THIS MIDI CHAIN** → **"CURRENT PART:
UNASSIGNED"** (the christening state as a status line; becomes
PART n; ADD PART beside it) → INPUT (summary chip + keyboard per
the standing rulings) → **THE CAST, aligned with the top 4 rows of
both grids** (the three surfaces share a register) → **THE CHAIN
AS A 4×2 FLOW BLOCK** (slots snake 1-2-3-4 / 8-7-6-5 — the
boustrophedon at block scale; the dotted thread through it; the
handle sits directly above the literal chain = see-it-name-it
perfected). The zigzag is dead; the column IS the machine.

## 2. TWO CONSOLES (the §10 console splits; §10b's diagonal dies
## spatially)
- **PART EMITTERS, directly below STAGING**: the part's wire
  choice — tap = set · hold = preview (§7/§10 grammar) — at the
  part's own grid.
- **SONG EMITTERS, directly below PLAY**: the piece's mixer —
  A–D strips, meters, CH labels, M/S, and **the part BADGES**
  (drag badge strip→strip = re-wire that part; tap badge = select
  it — §10b verbatim, now living where the whole song is).
Choose at the workshop; mix at the stage.

## 3. THE FUNCTION ROW
RANDOMIZE · STAGE THE GRID · MUTATE · 🎲 RE-ROLL consolidate into
one row (placement per the mock as it settles; STAGE stays
staging-adjacent per §9's verb-at-source).

## 4. VERBS REDUCED: PLACE, MOVE, CLEAR ALL — DROPPED
**DELETE survives** (the shared destructive verb, litter-successor
semantics intact). **THE PIN (lean, Paul confirms on glass): in
EDIT view, tapping an empty staging cell PLACES the selected
colour directly** — no arming, direct paint; row buttons still
fill; occupied-cell tap = select (feeding LAST TOUCHED); MOVE's
work = delete + re-place (or the drag returns later if missed —
tombstoned, not forgotten). One armed verb on the whole page:
DELETE. The workbench becomes direct manipulation.
— design-side Claude

## §5 — THE TARGET SYSTEM (Paul, 2026-08-13: multi-cell editing
## made visible)
- **Select a cell → the RETICLE**: the selected cell wears the
  primary mark (◎); **every instance of its colour everywhere** —
  staging, play grid, the cast swatch — wears the secondary (○).
  The blast radius is visible BEFORE any edit; editing stays live
  (colour-owned does the work; the icon does the consent).
- **Ink-drawn, lens-proof**: the marks are neutral ink, never
  hue-tinted — they must read THROUGH the band lens (an instance
  wearing band-2 teal still shows the same ○ as its gold sibling
  in staging).
- **The count rides the ID chip**: "◎ 6 cells" — the radius
  numbered where the identity already lives.
- **Cross-colour multi-select: DECLINED, with the reasons on
  record**: the model already has its grouping levels — COLOUR =
  the twin-set (this system's scope) · PART = the I/O group ·
  MACROS = cross-anything param groups. A fourth ad-hoc grouping
  would blur all three; if a need survives device time, it
  arrives as a macro, not a selection mode.

## §6 — RULINGS (Paul, 2026-08-13)
- **§5 THE TARGET SYSTEM: RATIFIED.** Build it as written.
- **§§1–4 (the layout restructure): DROPPED** — proposed, mocked,
  declined by Paul's eye. The mock served its purpose: a cheap
  trial, a cheap rejection. TOMBSTONE: the 4×2 in-column chain ·
  the two-console split · the cast-register alignment · the verb
  drops — all withdrawn. **The as-built layout stands**: the
  footer chain (its original tombstone re-affirmed) · the single
  output console per §10/§10b of the part-lifecycle file (strips
  under play; badges as handles) · PLACE/MOVE/DELETE as wired.
  Re-open any piece only by Paul's word.

## §7 — THE TWO-HANDED PERFORMER (Paul, 2026-08-13: keyboard in
## one hand, Launchpad/iPad in the other — a framing shift + one
## new design area)
- **Validation, on record**: the architecture already serves this
  — live-first pool (live ∪ latch → house → reference), boundary-
  deferred picks under sustained chords, the derive-law
  re-answering every chord change instantly. The piece's
  independence (the exclusivity correction) is what makes the
  left-hand-plays / right-hand-arranges duet possible at all.
- **Framing re-ranks**: LATCH = the third hand (not the primary
  input); the house chord = the rehearsal stand-in; the manual's
  performance chapter leads with the duet, not the plugged chord.
- **★ THE NEW DESIGN AREA (opened, not specced): CONTROL-IN** —
  the play grid's performance verbs (rung picks · FREE voicing ·
  lane mutes · scene switches · fifth-row actions when they land)
  as MIDI-MAPPABLE targets: the Launchpad plays the play grid.
  Two routes exist and must stay distinct (AUM's control matrix
  vs plugin-internal learn — the standing layering note); the
  hygiene law: **a control door is never a music door** (separate
  port/channel, documented). Design session owed when Paul calls
  it.

## §8 — THE ROWS-ONLY LAW (Paul, 2026-08-13: teach rungs; kill
## cell-painting on staging — supersedes §9b's woven staged rows)
- **STAGING PLACEMENT IS ROWS ONLY**: a colour is placed as A ROW
  (the row buttons are THE placement gesture; individual-cell
  placement on staging is REMOVED). The newcomer's cell-painting
  pull (arp·arp·ratchet across one line) is redirected by
  construction — you cannot do the confusing thing.
- **CELL TAPS ARE PICKS, never placement**: tapping cells toggles
  PRESENCE per column — the horizontal/structural choice (and in
  PLAY view this IS the SINGLE rung-pick; one concept, both
  views). Rows = candidates · columns = structure.
- **THE WEAVE MOVES TO FLATTEN**: mixed-colour lines are BORN AT
  DEPLOYMENT — the picks across rows/columns ARE the weaving act;
  FLATTEN assembles the line from them. §9b's woven STAGED rows
  retire (staged rows return to ONE COLOUR PER ROW — the §9
  original: STAGE fills rows with variations; manual stacking
  fills them with chosen machines; one mental model either way).
- **The teaching line (manual + first-light)**: "Stack machines as
  rows. Pick where each speaks. Flatten weaves the line."
- Verb ripple: on staging, DELETE at row-grain (the row button's
  clear state covers it); PLACE/MOVE lose their staging cell-scope
  (Paul's earlier drop instinct, now confirmed by the law).

## §9 — RECONCILIATION (2026-08-16): §4/§6's verb roster SUPERSEDED
## by Paul's direct instruction to Code (device outranks; absorbed)
- **EDIT mode is RETIRED on the part grid.** PLACE/MOVE/DELETE and
  the EDIT view are gone from staging.
- **The verb box = a ROW-MODE RADIO: SELECT · PLACE · MUTATE** —
  it changes what the LEFT ROW BUTTONS do: SELECT = whole-row
  rung-pick across columns · PLACE = stamp the selected colour
  across the row (target icon in the colour) · **MUTATE = MINT a
  value-only variant colour** (≤3 nudged params, fingerprint incl.
  velocity+gate, distinct from source AND every row, unique hue,
  pulses in the palette as a create-me candidate).
- §8's rows-only law stands as built ✓. Vocabulary care, flagged
  once: the row-mode MUTATE **mints**; the dice-family mutate
  **walks in place** — the manual must split them (or the row mode
  renames to VARY/MINT if confusion bites on glass). Paul's call.
- Also absorbed: **the unassigned part SAVES with the document**
  (option A; per-scene deferred with scenes v2).
