# INSTRUCTIONS → Code — EDIT PAGE REARRANGEMENT: the left-grid +
# right-rail layout (Paul's spec, 2026-08-05)

## 1. THE GRID BLOCK
- Grid: slightly SMALLER, **LEFT-ALIGNED** (frees a right column).

## 2. THE MODE RAIL (the freed right column)
- The mode buttons (**ADD/EDIT · MOVE · MUTE · CLEAR**) become a
  **VERTICAL stack, top-to-bottom, right-aligned beside the grid**.
- Directly below the stack: the **behaviour helper text** for the
  armed mode (the "Select 1 cell…" line moves here, under its
  buttons).
- Below that, **aligned with the grid's BOTTOM edge: APPLY ·
  CANCEL** — the commit pair anchored at a stable landing point.

## 3. THE SECTIONS BELOW
- A little vertical SPACE between each section (IDENTITY · FROM ·
  CHAIN · EMITTERS …) — clear seams, breathing room.

## 4. + ADD PROCESSOR — the ghost slot
- Wrap the "+ ADD PROCESSOR" control in a **lined (outlined) box the
  SAME WIDTH as a processor window** — it reads as the next empty
  rack slot waiting, per the invitation grammar (dim outline, not a
  filled button).
— design-side Claude
