# INSTRUCTIONS → Code — REDESIGN FIXES: the flow line · the rack's
# two verbs · the quiet panel (Paul + design, 2026-08-17)

## 1. THE CHAIN PANEL — numbers out, the flow line in (Paul's
## plan, endorsed + one duty preserved)
- Empty slots lose their numerals → **ghost-dashed empties** (the
  house grammar). Populated slots show their names (ARP, RATCHET…)
  as today's plan.
- **THE FLOW LINE inherits the numbers' secret job: ORDER.** In
  the 2×4 block the dotted thread must draw the path unambiguously
  — door → slot 1 → … → slot 8 → wire, **with the turn glyphs at
  the row wraps** (the boustrophedon made visible). The panel
  becomes the machine's anatomy in miniature: MIDI IN A ┈▶ [the
  slots] ┈▶ MIDI OUT A.

## 2. THE RACK — two verbs, two dresses (the confusion named)
- **MEMBERSHIP (on the emitter strips)**: the RACK button becomes
  a **membership MARK** — the rack's glyph/bracket on the strip,
  lit when this wire is in (a badge, not a button-word). It edits
  membership OF THE ACTIVE CONFIG.
- **CONFIGURATION (the RACK 1–4 row)**: reads as ONE control —
  **"RACK ▸ [1][2][3][4]"** (a labelled radio: which rack setup is
  live). It RELOCATES to the rack's own side (with/above the out-
  strips), out of the workshop's function row — config lives at
  the thing it configures.
- The relationship, one line for the manual: "The radio picks the
  rack's setup; the marks on the wires show (and edit) who's in
  it."

## 3. THE LEFT BOX — the channel law rules the colour
The full-saturation hue frame is a CHANNEL COLLISION (frames =
voice/zone duty; hue = machine duty). Fix: **kill the loud box** —
- the machine's hue speaks through the selected chip, the
  populated slots' tint, and **a thin (~2pt, low-alpha) hue SPINE
  on the panel's left edge** — the thread law's original form;
- the panel's border returns to neutral chrome (quiet), gaining
  the voice accent ONLY when the chain audition is sounding (the
  frame doing its own job).
Identity present, chrome quiet, no channel steals another's duty.
— design-side Claude
