# SPEC → Code — ARP · CYCLES + THEN (Paul, 2026-08-27 — the
# phrase controls; captured, awaiting his word)

## The pair (one feature: the arp learns to END)
- **CYCLES: ∞ (default, today untouched) | 1 | 2 | 3 | 4** —
  full pool-traversals (one cycle = one complete pattern pass,
  rests included when the mask is on), counted from the
  derivation window's entry.
- **THEN: REST | HOLD LAST | HOLD FIRST | HOLD ALL** — the
  landing, ON the arp card (Paul's navigation complaint cured:
  no trip to LENGTH/gate):
  - REST — the arp as ornament: a flourish, then air.
  - HOLD LAST — climb, then ring on the landing note
    (arp-into-drone).
  - HOLD FIRST — return to the root and sit.
  - HOLD ALL — the pool blooms (arp-into-pad; the cascade
    finish).

## Pins
- THEN is HIDDEN at CYCLES=∞ (the animate-in law — the ending
  appears when an ending exists).
- **NEW CHORD: RETRIG restarts the count** (a fresh chord = a
  fresh phrase; the musical default). FREE lets the count run
  through changes.
- Holds ride the tie/gate machinery (strike-per-span's sibling —
  cycles-then-sustain vs columns-then-sustain; one engine
  family, noted so nothing double-builds).
- The euclid mask composes: a cycle = one full masked pattern.
- Release at the window's end per standing LENGTH laws.
— design-side Claude

## §2 — THE PHRASE FAMILY (Paul's question, 2026-08-27:
## CYCLES + THEN generalises to the drivers)
One engine (the cycle counter + the tie landing), many cards —
ranked by destiny completed:
1. **CASCADE** — CYCLES 1 + HOLD ALL = build once, ring: the
   swelling pad entrance. The card's true form.
2. **STRUM** — one rake, let it ring (1 + HOLD ALL): the
   guitarist's actual gesture.
3. **MOTIF / RIFF** — the phrase once, then REST or HOLD LAST:
   the fill machine (fires per chord change, gets out of the
   way — RETRIG restarts, as ARP).
4. **EUCLID** — N figure-cycles then rest: euclidean
   call-and-response. (Card-level; per-line stays simple.)
5. **RATCHET · TUTTI PATTERN · WEAVE** — roll/figure/lattice N
   times, then land or rest.
6. **MOD (the value cousin)** — LFO CYCLES then FREEZE at the
   final value: the settling modulation (composes with ON EXIT;
   flag so the two chips don't fight).
Not invited: ECHO (repeats are its count) · set-shapers (nothing
to cycle) · DRONE (already the hold). THEN's options render
per-card (HOLD ALL on mono walks = the pool blooms, legal).
The chip pair ships as ONE shared component; cards opt in.
