> SUPERSEDED by AcceptanceCriteria-build-page-iteration-6.md §9 (2026-08-16): the PLACE·MOVE·DELETE verb roster + EDIT view described below were tombstoned; the part grid now uses the SELECT·PLACE·MUTATE row-mode radio and a cell tap is a rung pick. Kept as historical record.

# INSTRUCTIONS → Code — BUILD PAGE ITERATION 3 (Paul, 2026-08-11)
# Supersedes iteration 2's §3 (the bar-absorption is REJECTED —
# "at the bottom it just looks wrong, and the cast doesn't have
# enough options"). The updated mock travels with this file:
# `mockup-three-grids-landscape.html` — build to it.

## 1. THE LEFT COLUMN STAYS — reordered to Paul's sequence
Top → bottom:
1. **[● PLAY THIS CELL]** — first thing in the column; the machine
   zone's audition AND the focus lamp's handle (pulls the voice
   left, per the focus model).
2. **[PART 1 ▾] [+ NEW]**
3. **1 · INPUT** — R1–R4 chips (MIDI ⎓ | PIANO ⌨ per door; the
   octave keyboard reveals under a selected PIANO door).
4. **2 · THE CAST — the FULL 4×4 palette** (sixteen slots; the
   compact-bar version starved it — grid form is canon).
5. **3 · OUTPUT** — A–D chips.
6. **[APPLY TO STAGING →]** — the column's closing word, pink,
   pointing at its destination.
7. LITTER parks beneath (placement revisits later, per Paul).

## 2. CHAIN has no numbered step — THE SNAKE BELOW IS THE CHAIN
The column says who · through what door · to where; the machinery
bar says what happens in between. The bar keeps: the snake (ID ·
receiver box · slots + ghost · emitter box) + RANDOMIZE at its
right. (PLAY THIS CELL leaves the bar — it moved to the column
top.)

## 3. STAGING'S VERBS live UNDER STAGING
**[APPLY TO PLAY →] · [MUTATE] · [🎲 re-roll]** — the workshop's
strip beneath its own grid.

## 4. Kept from iteration 2 (unchanged)
- **THE TARGET DECIDES THE VERB**: APPLY TO PLAY arms the bands;
  LANE = flatten · LADDER = copy rows · FREE = stock takes;
  long-press a ladder = flatten-into-ladder (the rare alternate).
- The focus economy + the ● PLAYING frame + the dual-behaviour
  staging (instrument vs workbench) as shipped.
- The right/bridge column is GONE — its width flows to the two
  grids.

## The acceptance read
The page reads left → right as build · stage · deploy, every verb
standing at its own front door, the cast at full size, and the
column closing on the arrow that starts the journey.
— design-side Claude
