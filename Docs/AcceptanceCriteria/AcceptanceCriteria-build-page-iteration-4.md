> SUPERSEDED by AcceptanceCriteria-build-page-iteration-6.md §9 (2026-08-16): the PLACE·MOVE·DELETE verb roster + EDIT view described below were tombstoned; the part grid now uses the SELECT·PLACE·MUTATE row-mode radio and a cell tap is a rung pick. Kept as historical record.

# INSTRUCTIONS → Code — BUILD PAGE ITERATION 4 (Paul, 2026-08-11)
# The mock travels updated. Four rulings:

## 1. GRIDS SMALLER, PAGE CALMER
Staging + play cells drop to ~34pt (mock shows it); gaps tighten.
The play grid CALMS (its full-saturation mosaic was the busyness —
mock dims its cells ~18%; Code may prefer desaturating dormants).
Chrome-quiet applies to the whole page: the workshop should feel
spacious, not dense.

## 2. RANDOMIZE RELOCATES — it is the CHAIN's verb
It leaves the machinery bar's right end and joins the LEFT COLUMN
at the cast/chain step (🎲 beside THE CAST header in the mock;
exact seat = Paul's eye). Two dice now exist, each at its source:
**column 🎲 = roll the selected colour's machine · under-staging
🎲 = re-roll the variation ladder.** Label them distinctly.

## 3. ★ THE VERBS RETURN — drag retires on BUILD
Paul: "drop the drag & drop, use verb buttons — PLACE, MOVE,
DELETE." The spring-held grammar (the house law) replaces the
custom drag on this page:
- **PLACE** — hold/arm, tap staging cells = place the selected
  cast colour (row-selector fill still works alongside).
- **MOVE** — arm, tap source then destination (overwrite per the
  standing ruling).
- **DELETE** — arm, tap cells to clear.
Spring semantics per the verb law (hold → taps do → release done);
all workbench-state (silent), all one-undo-per-action. This also
retires the AU-host drag fragility and its ghost chrome. FORK and
ADOPT park (no drag vehicle; they return later if missed — likely
as cast-side gestures). **The DRAG&DROP tab retires when these
land** (BUILD supersedes it, as planned).

## 4. The verb strip sits UNDER STAGING, above the apply row
[PLACE · MOVE · DELETE] / [APPLY TO PLAY → · MUTATE · 🎲] — the
workbench's verbs, then the workshop's outcomes.
— design-side Claude
