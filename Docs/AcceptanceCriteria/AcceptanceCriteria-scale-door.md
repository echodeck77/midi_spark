# SPEC → Code — THE SCALE DOOR MODE (Paul, 2026-08-26 — captured,
# awaiting his word)

## The sixth mode: LATCH · HOLD · KEYS · REPLAY · FILE · SCALE
The door's pool = EVERY NOTE OF A CHOSEN SCALE — a standing
scale-pool, no playing or typing required.
- **Sheet controls**: ROOT (C–B) · SCALE (curated list: major ·
  natural/harmonic/melodic minor · dorian · mixolydian ·
  lydian · phrygian · major/minor pentatonic · blues ·
  whole-tone · chromatic) · **RANGE** (the home octave window,
  default 2 oct — rank-walkers need a defined ladder; the
  pitch-class set realized across it).
- **Engine = KEYS with a picker**: a scale is a pre-typed pool;
  the machinery is identical, the sheet is friendlier. Cheap by
  construction.
- **THE COMPLEMENT COMBO (the point)**: SCALE + EXCLUDE [door] =
  the diatonic complement with ZERO typing — C major minus the
  held chord = always-in-key flourishes around the harmony. ONLY
  composes too (scale ∩ chord = consonance-locked). Two chips.
- **Philosophy pin**: this is DECLARATION, not inference — the
  key question's standing resolution intact; the instrument
  still never guesses.
- Later, free: NOTES-PER-OCTAVE (microtonal Tier 1) generalises
  the picker to EDO scales — same mode, wider list.
— design-side Claude

## §2 — POOL-STEP UNITS (Paul, 2026-08-26 — the synergy that
## pays the scale door off; captured with it)
**The law**: pitch offsets may count in POOL STEPS — degree
arithmetic against WHATEVER pool feeds the chain. One chip on
the offset-bearing cards: **UNITS: SEMITONES | POOL.**
- **HARMONIZE** — voices in degrees: +2 = the diatonic third,
  major or minor as the key dictates (the real harmonizer,
  no inference).
- **ECHO · PITCH STEP** — echoes that WALK THE SCALE as they
  fade (in-key trails).
- **TRANSPOSE** — "up a third in key."
- **GLIDE · STEP mode** — the zipper runs scale-tones, not
  chromatics.
- **RIFF** — needs nothing: ranks ARE pool degrees; born in tune.
- General by construction: a chord pool → chord-tone stacking; a
  complement → in-palette offsets; the SCALE door → diatonic
  everything. The declared palette pays out the whole diatonic
  set the key question refused to buy with inference.
- **Edge pin**: an input note outside the pool anchors at the
  NEAREST pool position, then steps (the FOLD family's law).
Also on record: §3 of OPEN-listening-set (EXCLUDE door-only) is
RATIFIED per Paul, 2026-08-26.

## §3 — LIVE INPUT RESTRICTED BY A DECLARED KEY (Paul,
## 2026-08-26 — already composed by ratified law + ONE new chip)
**The composition (nothing new needed)**: door A = LATCH with
**ONLY: B** · door B = SCALE (the key). A's played input admits
only B's set — wrong notes never enter the pool. Filtering is at
the door (before pool, chain, and passage alike); pitch-class
per the CLAIM law. The LATCH generalisation + ONLY + the SCALE
door, composing exactly as ratified.
**The one new chip — REJECTS: BLOCK | SNAP** (on the ONLY/MINUS
filter):
- **BLOCK** (today's ratified nature) — the out-of-key press is
  silence. The strict gate.
- **SNAP** — the press sounds as the NEAREST note in the
  referenced set (the FOLD/nearest family): every key lands
  somewhere legal. The unmissable keyboard; the jam-proof door.
Manual line: "B holds the key; A can only speak it — blocked, or
gently corrected."

## §4 — THE DYNAMIC REFERENCE (Paul's probe, 2026-08-26: chords
## played through the key door — NOT chaos; the second instrument)
- **B moving = A chord-locked** (vs scale-locked): A's legal set
  = B's current harmony, re-deriving live (reconcile/never-lurch
  govern sounding notes as pools already do).
- **SNAP + dynamic B = THE ALWAYS-RIGHT LEAD**: every press lands
  on a chord tone of the CURRENT change — the left hand re-tunes
  the right in real time. One-finger solos that follow the
  progression.
- **BLOCK + dynamic B = HARMONY-PRUNED MELODIES**: notes gate in
  and out as the chords move; a held note dies when the harmony
  abandons it. Stark, compositional, deliberate.
- **The dial, named**: B=SCALE → loose (7 legal) · B=live chords
  → strict (3–4, moving). Same two chips, key-lock to chord-lock.
- **THE CARRY PIN (required)**: the reference set PERSISTS across
  B's silences (the PEDAL law — lifting the left hand must not
  mute the right), refreshing on B's next material. Truly-empty
  reference = silence + the truth-strip tell ("waiting for
  INPUT B").

## §5 — RATIFIED WHOLE (Paul, 2026-08-26)
Everything since the last ferry is law:
- **THE SCALE DOOR** (§1: the sixth mode — root · scale list ·
  range; KEYS-with-a-picker).
- **POOL-STEP UNITS** (§2: UNITS SEMITONES|POOL on HARMONIZE ·
  ECHO PITCH STEP · TRANSPOSE · GLIDE STEP; nearest-anchor edge;
  the general any-pool law).
- **LIVE INPUT RESTRICTED BY DECLARATION** (§3: the LATCH+ONLY:B
  composition confirmed + the REJECTS BLOCK|SNAP chip).
- **THE DYNAMIC REFERENCE** (§4: chord-lock via a moving B; the
  always-right lead (SNAP) · harmony-pruned melodies (BLOCK) ·
  the loose↔strict dial · THE CARRY PIN (reference persists
  across B's silences, PEDAL law) · empty-reference tell).
Also ratified this stretch, already marked in their own docs:
HOCKET + the wire-as-source class remain CAPTURED-awaiting-word
as filed (not in this ratification unless Paul says otherwise);
EXCLUDE door-only (OPEN-listening-set §3) confirmed ratified.

## §6 — THE SCALE-AWARE IN STRIP (Paul's grain problem,
## 2026-08-29 — captured; leaner than the auto-processor)
**The grain law, from his emitter-D proof**: the DECLARATION
(root·scale·range) = door business, one truth, stays on the
sheet. The BEHAVIOUR (how a chain uses the stream) = CHAIN
business — the same palette feeds an avoider AND a full-palette
harmoniser side by side.
**The solution — no auto-processor, no slot eaten**: when a
chain's door is a SCALE door, its IN STRIP becomes scale-aware:
- The readout, plain: **"A MIXOLYDIAN — the full palette."**
- **PLAY: ALL NOTES | AVOID CLASHES** — one chip, per-chain
  ("skip notes that would rub against what's already sounding")
  = the settled AVOID's common case (ALL-SOUNDING · SKIP)
  armed from the strip; the full AVOID card remains for exotic
  sources.
- **change key →** — the jump-link to the door sheet (displayed
  here, OWNED there; the one-home law intact; nothing exclude-
  shaped relitigated).
The hidden-away complaint cured by visibility, the grain honoured
by seat.

## §7 — RATIFIED TONIGHT (Paul, 2026-08-29) + the self-naming
## stage + the key-lock generalisation
**RATIFIED, all of tonight**: the ▲▼ track-move + ghost previews
(redesign §9) · the GRID 8|16 setting · THE MACRO LANES ·
AVOID's settled shape (listening §4) · the scale-aware strip
(§6) with this revision: **THE SETTING BECOMES A STAGE, AND THE
SETTING IS ITS NAME** — arming AVOID CLASHES materializes a
slot-resident stage displayed as "AVOID CLASHES" (the chrome
law: the non-default announces itself in the machine; the strip
chip is its switch, the slot is its face). CHORDS upgrades from
wanted-soon to RATIFIED.
**Captured with his lean (not yet ratified)**: the same stage on
NON-scale chains = **LOCK TO KEY** — a declared key (picked in
the stage, or referenced from a SCALE door) + REJECTS BLOCK|SNAP
applied to the chain's OUTPUT, **seated at the chain's END**
(post-derivation cleanup — the force-to-key filter arriving at
the grain his emitter-D proof established: chain, never door).
Self-naming as ever: the slot reads "LOCK: A MIXO."

## §7b — TWO RULINGS (Paul's questions, 2026-08-29)
- **AVOID's sources, tiered (not overcomplicated)**: the strip
  chip = ALL SOUNDING only (one word, one tap). The A–D/wire
  SOURCE pick lives in the materialized stage's PANEL (the
  setting-becomes-a-stage law pays: simple surface, depth one
  tap in). Nothing added to the chip.
- **START ≠ END, and start is already covered**: input-locking
  = the DOOR's job (§3's ONLY composition — no stage dupe).
  **The LOCK stage is the END lock**: it catches out-of-key
  notes MADE mid-chain (chromatic transposes · fixed-interval
  harmonies · pitch-step echoes) — proof: [start-lock →
  TRANSPOSE+1] emits out-of-key; [TRANSPOSE+1 → end-lock] snaps
  back. Manual honesty: SNAP can collapse chromatic neighbours
  into doubled tones — the safety net's one cost.
