# SPEC → Code — STRIKE PER SPAN (Paul, 2026-08-27 — the span
# ladder's missing half; captured, awaiting his word)

## The gap, named
The SPAN ladder spans the PATTERN INDEX (which step across a
wider window) — every cell entry still strikes. Paul's want:
span the STRIKE — fire once at the span's origin, HOLD through
the remaining columns. Index-span ≠ strike-span; the ladder
shipped only the first.

## The chip — on every span-ladder holder
**STRIKE: PER CELL (today) | PER SPAN** —
- PER SPAN: the driver derives + fires at the span's FIRST
  column only; the following columns extend the gates (the
  LENGTH/tie machinery — no new note-ons; adoption/never-lurch
  own the seams). Release at span end (or the note's own LENGTH,
  whichever the card's laws say).
- The mechanism is BURST-CARRY generalised (launch + stretch),
  riding standing law — no new engine class.

## What it unlocks (the honest list)
- **DRONE + PER SPAN** = the true multi-column pad (chain-mode's
  per-column re-strike finally optional).
- **TUTTI/HARMONIZE holds** = voicings that ring across the
  window.
- Span 8 + PER SPAN = strike once per row lap (the one-hit wash).
- Odd spans + PER SPAN = long tones cycling polymetrically
  against the row — sustained polymeter, which the index-span
  could never say.
- ARP + PER SPAN = legal (first step fires, holds) — noted, not
  the point.
Display: the span bracket (idea 16) shows the held region; the
first cell wears the strike mark.
— design-side Claude

## RATIFIED (Paul, 2026-08-27)
STRIKE: PER CELL | PER SPAN is law as written — every span-ladder
holder; the CARRY mechanism generalised; the bracket shows the
held region.
