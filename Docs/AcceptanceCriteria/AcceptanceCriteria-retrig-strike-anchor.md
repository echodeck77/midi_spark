# FERRY → Code — TWO QUESTIONS UNTANGLED: the internal transport
# REVERTS · RETRIG becomes STRIKE-ANCHORED (Paul, 2026-08-27)

## ① The internal-transport-when-stopped: REVERT IT
Paul's own second thought is the law's: **stopped = silent.**
The internal transport that plays while host+handles are stopped
is ambient sound — the passage law's surprise machine returning.
Revert it. The SANCTIONED jam-while-host-stopped path already
exists and stays: **PLAY THIS MIDI CHAIN on the free-run clock**
— playing because asked, visible on the handle. (This composes
with FERRY-passage-law when it crosses; same spirit, no
conflict.)

## ② The real itch: the HARDWARE-ARP FEEL = a PHASE pin, not a
## transport feature
- **RETRIG anchors phase AT THE STRIKE — immediate, sub-step.**
  Play a chord mid-bar on a playing chain: the pattern starts
  NOW (step 1 at the strike), like every hardware arp. Phase =
  time-since-strike: `step = floor((beat − strikeBeat) × rate)`
  — strikeBeat is an input event's timestamp, so replay is
  exact. Pure by construction.
- **FREE stays beat-anchored** (the grid-locked groove; today's
  feel preserved as the alternative). LEGATO unchanged
  (adoption's continuing voices).
- Applies to the drivers wearing NEW CHORD (ARP first; the
  family follows the same pin).
- Note: strike-anchored patterns free-run against the bar
  (polymetric vs the grid) — that IS the hardware feel; FREE is
  the locked one. The chip finally means its two feels.
Manual line: "RETRIG: the arp starts when you do. FREE: it
rides the grid."
— design-side Claude

## §3 — THE CONTEXT (so the ask-then-revert reads as one story,
## not whiplash)
The sequence, honestly: Paul asked you directly (the second
lane) for an internal transport playing while stopped — his aim
was the HARDWARE-ARP FEEL: strike a chord, hear the arp NOW, not
at the next bar. On reflection this session he judged the
mechanism "more confusing than anything" and suspected
silent-when-stopped is clearer — which is also his own passage
law speaking. The design untangling found TWO questions fused:
- **Audibility when stopped** = the passage/transport law's
  domain → stopped is silent; the handle + free-run clock is the
  explicit jam path (already yours, already right).
- **When a playing arp begins** = a PHASE question → the
  hardware feel is STRIKE-ANCHORING, and the existing NEW CHORD
  RETRIG is its rightful home (§2's pin) — no transport feature
  needed at all.
So: the internal transport was a correct instinct aimed at the
wrong mechanism. Revert ①, pin ② — and the itch that started it
is properly scratched. His device ear judges RETRIG's immediacy
once it lands.
