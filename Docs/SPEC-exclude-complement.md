# SPEC → Code — THE COMPLEMENT DOOR: KEYS gains EXCLUDE (Paul,
# 2026-08-22 — assessed HIGH VALUE; captured, awaiting his word)

## THE CHIP (on the KEYS door sheet)
**EXCLUDE: OFF | A | C | D** — this door's effective pool = the
TYPED set MINUS the excluded receiver's current notes, live.
- Subtraction is by PITCH CLASS (the CLAIM precedent — a C
  played anywhere removes all Cs here).
- Re-derives as the excluded door's chord moves (boundary/
  reconcile laws apply as with any pool change).

## WHY (the use, named)
Type a palette into B (a scale, a mode, any set) · EXCLUDE A →
**B = the palette minus the chord = the flourish door**: chains
on B play neighbours, colour tones, approach notes — AROUND the
harmony, never doubling it, following every change. The key-
awareness gains (legal neighbour notes) WITHOUT key inference:
the user DECLARES the palette; the engine only subtracts. The
thesis untouched.

## Scope + notes
- v1 = KEYS mode only (typed sets are where declaration lives).
  Generalisation to LATCH-mode complements = noted, unbuilt.
- Composes: [flourish chains on B] + CLAIM on the melody =
  collision-free ornamentation; PEDAL(src:A) under B's dances.
- Manual line: "Type the notes B may use; exclude A, and B plays
  everything the chord isn't."

## RATIFIED (Paul, 2026-08-22)
The EXCLUDE chip is law as written — KEYS mode, pitch-class
subtraction, live re-derivation. The complement door builds.
