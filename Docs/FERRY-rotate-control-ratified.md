# FERRY → Code — ROTATE: the control ruling (Paul, RATIFIED
# 2026-08-24 — a delta; no existing doc changes)
The numbered button row RETIRES on every ROTATE control.
Replacement, both pieces:
1. **The nudge pair: ◀ n ▶** — tap = slide the pattern one step
   (wrapping); the current offset sits small between the arrows
   (the truth). Two buttons regardless of STEPS; the workflow =
   tap until it grooves.
2. **Drag the lane/matrix itself** = rotate by direct
   manipulation (horizontal slide, wrap at edges); the arrow
   pair doubles as the visible hint for the gesture.
Applies everywhere ROTATE exists (slice rows, matrices, euclid
lines — per-line = a compact ◀ ▶ at each row's end). Engine
untouched; render + interaction only.
