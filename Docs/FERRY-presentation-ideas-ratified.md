# FERRY → Code — PRESENTATION IDEAS: RATIFIED WHOLE (Paul,
# 2026-08-25 — a delta on REPLY-presentation-pass.md; the six
# rules + remap stand, these 32 land on top. All render-only.)

## CONTINUOUS VALUES
1. Vertical-drag MINI-KNOBS as the compact slider form (four fit
   where one slider sat).
2. DRAG-THE-NUMBER — the value is the control (max-compact ★★).
3. DETENTS at musical values (gentle snap + haptic tick).
4. DOUBLE-TAP-TO-ZERO on bipolar controls (FAVOUR·SHAPE·TILT).
5. FINE MODE — hold-then-drag = 10× precision.
6. UNITS IN THE VALUE ("120ms" · "+7st" · "3/16♪").
7. TOUCH-PREVIEW — touching FADE/SPREAD ghosts the curve/rake in
   the window.

## ENUMS
8. SELF-DRAWING CHIPS — WAVE chips show waveforms · ARP arrows ·
   TUTTI dot-stacks (finish the existing thought).
9. The teach-line under every mode (increment-2, confirmed).
10. LONG-PRESS ANY CHIP = its explanation popover.
11. SINGLE-SWITCH TOGGLES — ON/OFF pairs collapse to one
    lit/unlit chip.

## NUMBERS (extends the ratified nudge pair)
12. TAP THE VALUE = a transient big-picker overlay (grid16
    reborn as an on-demand overlay, two seconds, gone).
13. MUSICAL FORMATTING (note-fraction glyphs for delays/rates).
14. HOLD-TO-ACCELERATE on the arrows.

## PATTERNS (matrices + lanes)
15. THE PLAYHEAD SWEEPS THE MATRIX (the live step lit in place).
16. THE SPAN BRACKET — span drawn as a bracket over its columns;
    dragging the bracket edge IS the span control.
17. RESULT-GHOSTING — computed hits faintly drawn while nudging
    rotate.
18. LANE READOUT AT THE FINGER (the touched bar's value floats).
19. PINCH A LANE = proportional scale.
20. LANE-HEADER TAP = mute that line (audition rows in/out).

## HIERARCHY + TRUTH
21. ★ DEFAULTS RECEDE ON FIELDS — at-default controls dim;
    deviations brighten (the house law, applied to panels; the
    card's story = hero + deviations).
22. DOUBLE-TAP A FIELD LABEL = reset to default.
23. ★ HOLD BYPASS = MOMENTARY A/B (hear it in/out, one finger).
24. TOUCH-A-CONTROL → the OUT strip highlights what changes
    (the truth strips as a live diff).
25. CONDITIONAL CONTROLS ANIMATE IN (causality visible).
26. LONG-PRESS A CONTROL = bind to macro (the doorway).

## LAYOUT + NAVIGATION
27. STICKY HERO (pins under the header; ECHO's fold halved).
28. SECTION ANCHOR CHIPS on long cards.
29. OPTIONS cluster folds to MORE ▾ past four.
30. The CC PICKER: named + recent + ◀74▶ raw path.
31. KEYPAD OVERLAY — hold a value = exact entry.
32. THE HERO-GLYPH IN THE PICKER — each card's thumbnail = its
    characteristic pattern (the parked spice, now law).

Sequencing = Paul's word; the short-list he saw flagged as the
high-value first wave: **21 · 23 · 16 · 15 · 8**.
