# SPEC → Code — WRITE (Paul, 2026-08-30: render the pass as a
# MIDI clip — captured, awaiting his word)

> STATUS (2026-09-04): CAPTURED, NOT RATIFIED — do NOT build. Ferried from
> the design-side Claude via `_dear_claude_code/`, added to Docs verbatim.
> Holds for Paul's word before any code.

## The button + the stage (his mechanism, the law's best outing)
- **WRITE** on the part page. Pressing it materializes a
  **self-naming WRITE stage at the chain's head**, drawn wired
  DIRECTLY to the emitters (the flow line shows the tap across
  the whole chain). The stage's on/off = the ARM (toggleable,
  visible in the machine — chrome law; bypass = disarm; remove =
  gone).
- Engine truth: the capture rides the REEL's existing per-row
  recording — the stage is the per-part face + switch, not a
  second recorder.

## The render
- **Boundary-complete**: each pass writes when it FINISHES (the
  dashcam KEEP semantics at part grain; partials wait for the
  boundary).
- Notes carry their WIRES verbatim (the wire-identity law;
  a SPLIT-BY-EMITTER option can join later, WHOLE = default).
- **Clips land in the LIBRARY** — and are FILE-DOOR FOOD: the
  derive → write → replay loop (the instrument bottling its own
  output as material for its own doors).
- Deterministic by construction (clips = recordings of derived
  output; seeds referenced in metadata).
— design-side Claude
