# FERRY → Code — COMPLEMENT DOOR: ratified extensions (Paul,
# 2026-08-22 — a DELTA on the already-ferried
# SPEC-exclude-complement.md; that file is unchanged)

## RATIFIED ADDITIONS
1. **The chip becomes three-way: FREE | MINUS [door] | ONLY
   [door].** MINUS = as ferried (palette − door's notes, the
   flourish door). **ONLY = palette ∩ door's notes** — the door
   offers nothing but consonance: scale-lock by declaration, no
   key inference. One mechanism, both doors: "everything the
   chord isn't" and "only what it is."
2. **EXCLUDE/ONLY generalise to LATCH mode** (not just KEYS): a
   live player on B, collisions with A filtered in real time —
   the collision-free duet.
3. **The truth strip shows the carving**: B's keyboard silhouette
   visibly re-shapes as the referenced door's chord moves (the
   feature made watchable; render-layer).
4. **Engine nicety: the exclude lag** — small hysteresis before
   a released note re-enters the pool, so fast playing never
   flickers the set. Value tuned on glass.
5. Composition noted, no build: MOTIF's deck on a MINUS door =
   ornament vocabulary that never lands on the melody.

## Manual fodder (the six use cases, one-liners)
The flourish door · the tension pad (colour tones by
subtraction) · walking approach-tone bass (+PEDAL) · the
controlled wrong-note rail (chromatic − chord) · the mallet
shimmer (pentatonic − chord, collision-free) · per-scene
palettes (the flavour as an arrangement dimension).
