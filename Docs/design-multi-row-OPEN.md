# OPEN → Code — THE MULTI ROW (the select grid's top buttons +
# the play grid reflection) — Paul + design, 2026-08-27.
# ⚠ STATUS: NOTHING HERE IS RATIFIED. A design-in-progress record
# so the shape survives; no build orders anywhere in this file.

## THE IDEA (Paul's, two sessions of shaping)
The select grid gains TWO EXPORT PATHS = two ways to perform:
- **RIGHT ROW = EXCLUSIVE** — one chain to one part (the
  committed band; the already-ratified part-buttons-as-commit).
- **TOP ROW = MULTI** — eight addressable SLOTS: park a chain,
  keep browsing, layers accumulate. The stack REFLECTS on the
  play grid's top row for live performance. The use case that
  drives it: build interlocking arps/hockets DURING DISCOVERY,
  and still have them when the play grid is crafted.

## DECIDED SO FAR (Paul's answers — decided-in-discussion, NOT
## ratified)
1. **Persistence: SAVED with the session** (document state) —
   built early, available later.
2. **The tap = MUTE/UNMUTE** (both surfaces; shared state).
3. **Top-row LOOPING is EVICTED from the play grid** (dropped
   there; KEPT on the part grid). The multi row takes the seat.
4. **Scenes (question ④): BOTH behaviours become an OPTION**,
   Paul inclined toward **(a) SCENE-CAPTURED** — each scene
   remembers its own layer on/off states (stacks compose into
   the arrangement) — with **(b) SCENE-INDEPENDENT** (the row
   rides above scenes; a live second deck) stored as the
   alternative. An option/chip decides; default lean = (a).
5. **I/O at park: FREEZE** — the slot keeps whatever
   door/wire context it was auditioned with.
6. **Traffic: COPY/PASTE both ways** between the select grid,
   its top/side buttons, and (implied) parts — the clipboard,
   not moves.
7. **Slot faces: TO BE DECIDED** (mosaics are DROPPED — Paul's
   word; not the answer here).
8. **The speaker/transport question: TO BE DECIDED** — what
   makes a multi layer "playing" under the passage law (the
   play-grid handle governing it vs its own life). THE key open
   design question.
9. **Capacity: ALL EIGHT playable** simultaneously (the
   governor watches; no artificial cap).
10. **The name: TO BE DECIDED** (MULTI = working title; the
    no-metaphors pass christens).

## OPEN (the remaining conversation)
Slot faces (7) · the speaker question (8 — shapes everything) ·
the name (10) · the exact scene-option chip's wording · whether
parts can be pulled UP into slots (6's reverse leg, implied but
unconfirmed) · the play-grid row's rendering (eight slots' states
at a glance).
— design-side Claude, recording; Paul's word ratifies, none given.
