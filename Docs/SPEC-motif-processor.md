# SPEC → Code — MOTIF (Paul, 2026-08-22: "a simple library of
# motifs" — the deliberately-simple sibling of RIFF; captured,
# awaiting his word)

## THE CARD (one sentence): RIFF's engine, playing from the
## library instead of an editor.
- **THE DECK = the shared riff library** with CHECKMARKS: tick
  the motifs that are in this cell's deck. One library serves
  both cards — RIFF makes motifs (draw/capture/save), MOTIF
  plays the collection. Factory packs land in the same library
  when curated (content, not code).
- **MODE — the whole dealer, three chips**:
  - **CHOOSE** — one motif, picked by hand (the list is the
    control; v1's heart).
  - **CYCLE** — through the checked set in order (a solo built
    from phrases).
  - **NEW CHORD** — deal the next on each harmony change
    (boundary-deferred; how improvisers think).
- **Inherited unchanged from RIFF**: WRAP (FOLD|CLAMP|WRAP) ·
  FRAME (HELD|FOLLOWING) · the stencil format (chord-agnostic by
  construction — every motif in the deck follows the harmony,
  free).
- NOT in v1 (the simplicity fence): tagging, weighting,
  per-motif overrides, seeded-random mode. The fence is the
  feature.
Storefront line: "Plays phrases from your library — every one
follows the chord you hold."

## §2 — PAUL'S CONTROL RULING (2026-08-22): three controls, done
- **The panel = THE DECK LIST + RATE + RESTRIKE|LEGATO.** Nothing
  else surfaces.
  - **RATE** — the ArpRate family; the motif's rhythm at speed.
  - **RESTRIKE** — every note re-attacks (the picked phrase).
  - **LEGATO** — notes overlap into each other (the smooth
    phrase; overlapping offs drive synth-side portamento — the
    GLIDE SYNTH interlock arrives free: legato motifs slide).
- **The MODE dealer (CHOOSE|CYCLE|NEW CHORD) moves behind the
  fence** — v1 selection = the list itself (tap a motif, it
  plays). CYCLE/NEW CHORD revive on Paul's word only.
- **WRAP and FRAME become silent defaults** (FOLD · FOLLOWING —
  following, because motifs-over-changes is this card's point);
  neither appears on the panel unless the glass ever demands it.

## RATIFIED (Paul, 2026-08-22)
MOTIF is law at §2's weight: the deck list · RATE ·
RESTRIKE|LEGATO; silent FOLD/FOLLOWING defaults; the dealer
fenced. The riff family completes: RIFF edits, CAPTURE records,
MOTIF plays the collection.
