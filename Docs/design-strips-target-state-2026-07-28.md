# Design guidance — THE STRIPS: complete target state (user priority wave, 2026-07-28)
_The user wants the receiver/emitter strips FINISHED as one wave — touched once and done. Every standing
ruling assembled here as the definitive spec. Single-face forever (config lives in the cog); pressable and
playable within the existing footprint. Already-ratified law — nothing new to question; build the wave, the
user's hands verify. (Consolidated from the design-side GUIDANCE ferry + the DUCK ruling from the
duck/adoption reply.)_

## THE RECEIVER STRIP (A–D)
- **Header**: hue dot · label · THRU pip (radio — labeled on the holder, empty circles on the rest).
- **LATCH** — the arm, RESTORED to the strip (performance control, per the split law): lit when armed; label
  reads **LATCH / LATCH+** per the cog's CHORD|ADD mode. Reachable, inviting, top of the feature column.
- **OCT− / OCT+** — ephemeral nudges, spring class.
- **The slider** = input velocity override (momentary-absolute; springs; HOLD-capturable; **bottom =
  fader-kill**) with **VELOCITY MARKS** metering (the ratified redesign: a mark AT each strike's height, holds
  while sounding, fades ~250ms on release; the FILL returns only while touching; strip identity hue).
- **Foot**: LIVE · SOLO — muted state must FAIL LOUDLY (dim strip + the label flips to **MUTED**, not merely unlit).
- **THE SESSION FACE** (during a PLACE/SELECT hold): the whole strip becomes one large **ROUTE IN** target —
  content dims beneath, hue glows, big label, current source wears a solid ring, candidates breathe; instant
  restore on release/cancel. Nothing floats; the strip IS the button.

## THE EMITTER STRIP (A–D)
- **Header**: hue dot · label · ch.
- **Roles**: CLAIM · **DUCK** · ALT — all in the roleButton grammar (tap = toggle · vertical drag = the one
  param: leak % · duck amount · alt count) + OCT±.
  - **DUCK = the ruled rename of FLAT** (user, decisive): button label + spec prose only ("activity ducking"
    language already fits). **Code identifiers stay** — `flattenMask`/`flattenAmount` etc. (the no-rename rule
    holds). Applied with this strip pass.
- **The slider** = output velocity override (same spring/HOLD/fader-kill laws) with marks **tinted in the
  SOURCE CELL'S Colour** (the cargo law reaching the meters — who struck, how hard) + the **hollow withheld
  tells** (already shipped) riding on top.
- **Foot**: LIVE · SOLO (same loud-mute law).
- **THE SESSION FACE**: **ROUTE OUT** — the bus toggles large and geographic (lit = enabled, tap = toggle,
  state visible on the face).

## BUILD ORDER (the user's priority wave)
1. **Session-faces polish** to the spec above — dim-beneath · hue glow · whole-strip targets · breathing ·
   meters-beneath · restore; **the compact route bar fully retires** (session faces partly shipped in `1792627`
   — bands already wear ROUTE IN/OUT during holds; this finishes them).
2. **LATCH restoration + the DUCK relabel** (small).
3. **The VELOCITY MARKS redesign** on both strips — finish the metering once, with the withheld tells preserved.
4. **The polish-laws pass on the strips** — chrome quiet: LIVE pads and role lights drop a saturation step;
   defaults recede, so the band reads calm at rest and alive where music moves.

_Not this wave: the ALT baton + entry-pulse ambience (the later per-panel process-view pass)._

## ADDENDUM (user, 22:42 frame 2026-07-28) — SPACE-FILL + the verbs ruling
AUDIT vs this doc: **DUCK ✓** landed · **LATCH ✓** restored (padlock, inviting) · roles/single-face/THRU ✓.
Outstanding-as-queued: marks (③) · chrome-quiet (④) · session-face polish (device-verify).

**DIVERGENCE — the user's ruling: the verbs are PILLS, not ROUND.** "Round and inviting" was the word —
enforce **circles** on the verb cluster (PLACE/HOLD · DELETE/SELECT · COPY/PASTE) or the user amends the word.

**THE SPACE-FILL DIRECTIVE — dead space is spent on TOUCH, not content** (every reclaimed pixel goes to
something a FINGER touches or a MARK draws — never to new controls; gutters tighten accordingly):
- **MIDI INPUT / MIDI OUTPUT boxes GROW to fill their bands**; inside, the strips stretch VERTICALLY — taller
  sliders (the marks get room; touch improves), the feature column distributes over the height (header ·
  LATCH/roles · OCT · foot spread, no crowding, no voids).
- **MASTER fills its column**: the fader gets full height beside a distributed feature stack (MUTE · KEY ·
  REVERT · INS seats spaced).
- **CONTROLS spends its dead area on HOLD** — the performance latch grows into the empty region below it
  (the corner's headline control; thumb-sized dignity).
