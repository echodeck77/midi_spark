//  EffectiveParamsTests.swift
//  RETIRED (CELL MACHINE): the A/B morph interpolation these covered was removed — the effective* helpers now
//  read the single (A) param bag, and the render reads the per-cell chain directly. Kept as an empty case so the
//  file needn't leave the test target (project globs Tests/); re-home real coverage in RouterTests if morph ever
//  returns as a slot-pair feature.

import XCTest

final class EffectiveParamsTests: XCTestCase {}
